# RoomRoute CRM

RoomRoute CRM is a full-stack customer relationship management app.

## 📁 Project Structure

```
roomroute-crm/
├── backend/       # Express + TypeScript + Prisma backend
│   ├── src/
│   ├── prisma/
│   ├── package.json
│   └── tsconfig.json
└── frontend/      # (Coming soon) React + Vite frontend
```

## 🚀 Getting Started

### Backend

1. Add your `.env` file using `.env.example` as a guide  
2. Install dependencies: `npm install`  
3. Build the app: `npm run build`  
4. Start the server: `npm start`

### Deployment

This app is built to deploy on [Railway](https://railway.app)

## 🧠 Features (Coming Soon)

- Email + Password login
- Google, Apple, Facebook login
- CRM dashboard
- Contact and note management

